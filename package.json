{
  "name": "subscription-tracker",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "pnpm --filter database build && pnpm --filter backend build && pnpm --filter frontend build",
    "dev": "concurrently \"pnpm --filter frontend dev\" \"pnpm --filter backend dev\"",
    "build:web": "pnpm --filter frontend build",
    "build:api": "pnpm --filter backend build",
    "start": "pnpm --filter database db:deploy && pnpm --filter backend start"
  },
  "keywords": [],
  "author": "",
  "license": "GPL-3.0-only",
  "packageManager": "pnpm@10.10.0",
  "dependencies": {
    "concurrently": "^9.2.1",
    "dotenv": "^16.5.0"
  },
  "devDependencies": {
    "@types/node": "^25.9.1",
    "nodemon": "^3.1.14"
  }
}
